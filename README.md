An Emotion-Based Music Recommendation System is a smart application that detects a user’s emotional state (like happy, sad, angry, etc.) and suggests songs that match or improve that mood 🎧
Technologies Used:
Python
OpenCV (image processing)
MediaPipe (face detection)
TensorFlow/Keras (model training)
NumPy & Pandas (data handling)
Streamlit (UI)


(Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned) ; (& d:\Programs\Projects\Emotion-based-music-recommendatipy -3.11 -m venv venv                              
>> venv\Scripts\activate                                                                               
>> python -m pip install --upgrade pip                      
>> pip install -r requirements.txt